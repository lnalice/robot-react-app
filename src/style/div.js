import styled from 'styled-components';

const BasicContainer = styled.div`
  display : block;
  text-align : center;
  max-width: 500px;
  margin: 0 auto;
  padding: 20px;
`;

export default BasicContainer;